<div class="main">

<h1 class="portfolio-header"> Dashboard </h1>

  <div class="dashboard-top">
    <div class="dashboard-box-1">
      <p class="header"> PORTFOLIO VALUE </p>
      <p class="value"> <%= number_to_currency(@current_value) %> </p>
    </div>
    <div>
      <p class="header"> PERFORMANCE </p>
      <p class="value pos-neg"> <%= number_to_currency(@performance_in_eur)%> </p>
    </div>
    <p class="performance pos-neg"> <%= @performance_in_percent %> % </p>
    <!-- <p> Your total investments: <%= number_to_currency(@initial_invest) %> </p> -->
  </div>


<div class="tvwrapper">
  <div id="tv-chart" data-day="<%= @day_data.to_json %>"
                    data-week="<%= @week_data.to_json %>"
                    data-month="<%= @month_data.to_json %>"
                    data-year="<%= @year_data.to_json %>">
  </div>
</div>

<div class="sectionheader">
<p class="sectionheader1"> INVESTMENTS</p>
<p class="sectionheader1"> PERFORMANCE </p>
</div>

    <ul class="detailslist">
    <% current_user.assets_by_name.each do |hash| %>
      <li class="details">
        <div class="details2">
        <p class="detailsheader"> <%= hash[:name] %> </p>
        <p> <%= number_to_currency(hash[:portfolio_value]) %> </p>
        </div>
        <p class="pos-neg"> <%= hash[:performance].round(2) %> % </p>
        <!-- | Portfolio share: <%= hash[:portfolio_share].round(2) %> % -->
      </li>
    <% end %>
  </ul>

<div class="sectionheader">
  <p class="sectionheader1"> SECTORS </p>
  <p class="sectionheader1"> PORTFOLIO SHARE </p>
</div>

    <ul class="detailslist">
    <% current_user.industries_by_name.each do |hash| %>
      <li>
        <div class="details">
          <p class="detailsheader"> <%= hash[:name] %> </p>
          <p> <%= hash[:portfolio_share].round(2) %> % </p>
        </div>
          <dl class="percentage percentage-<%= hash[:portfolio_share].round(0) %>"> </dl>
          <p> <%= number_to_currency(hash[:portfolio_value]) %> </p>
      </li>
    <% end %>
  </ul>

<div class="sectionheader">
<p class="sectionheader1"> ASSET CLASSES </p>
<p class="sectionheader1"> PORTFOLIO SHARE </p>
</div>

  <ul class="detailslist">
    <% current_user.categories_by_name.each do |hash| %>
      <li>
        <div class="details">
          <p class="detailsheader"> <%= hash[:name] %> </p>
          <p> <%= hash[:portfolio_share].round(2) %> % </p>
        </div>
        <dl class="percentage percentage-<%= hash[:portfolio_share].round(0) %>"> </dl>
        <p> <%= number_to_currency(hash[:portfolio_value]) %> </p>
      </li>
    <% end %>
  </ul>

<h6 class="sectionheader1"> YOUR PORTFOLIOS </h6>

  <ul class="detailslist">
    <% current_user.portfolios.each do |portfolio| %>
      <div class="institutions">
        <li> <%= link_to portfolio.institution.name, portfolio_path(portfolio) %> </li>
        <li class="view"> <%= link_to "view", portfolio_path(portfolio) %> </li>
      </div>
    <% end %>
      <li class="institutions" > <%= link_to "+ Add", institutions_path %> </li>
  </ul>


<!-- <h4> Your assets: </h4>

  <ul>
    <% current_user.assets_by_name.each do |hash| %>
      <li>
        <%= hash[:name] %>
        | Portfolio value: <%= number_to_currency(hash[:portfolio_value]) %>
        | Portfolio share: <%= hash[:portfolio_share].round(2) %> %
        | Performance: <%= hash[:performance].round(2) %> %
      </li>
    <% end %>
  </ul> -->

<!-- <h4> Sectors: </h4>

  <ul>
    <% current_user.industries_by_name.each do |hash| %>
      <li>
        <%= hash[:name] %>
        | Portfolio value: <%= number_to_currency(hash[:portfolio_value]) %>
        | Portfolio share: <%= hash[:portfolio_share].round(2) %> %
      </li>
    <% end %>
  </ul> -->

<!-- <h4> Asset categories: </h4>

  <ul>
    <% current_user.categories_by_name.each do |hash| %>
      <li>
        <%= hash[:name] %>
        | Portfolio value: <%= number_to_currency(hash[:portfolio_value]) %>
        | Portfolio share: <%= hash[:portfolio_share].round(2) %> %
      </li>
    <% end %>
  </ul> -->

</div>

