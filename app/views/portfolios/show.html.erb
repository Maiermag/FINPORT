<h1> <%= @portfolio.institution.name %> </h1>

<%= link_to "< go back", "#" %>

<% total_invest = 0 %>
<% @assets.each do |asset| %>
  <% asset.acquisitions.each do |acquisition| %>
    <% total_invest += (acquisition.unit_price_bought * acquisition.units_bought)%>
  <% end %>
<% end %>

<h3> Your investments: <%= total_invest.round(2) %> € </h3>

<h3> Your performance: </h3>

<h3> Industries: </h3>

<h3> Assets: </h3>
